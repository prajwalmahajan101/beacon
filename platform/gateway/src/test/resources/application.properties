# Test overrides layered on top of the main application.yaml (merged, not replaced).
# Bind OS-assigned ports so multiple cached Spring test contexts never clash on the fixed
# OTLP/gRPC port (4317) or the actuator management port (9464).
beacon.gateway.grpc-port=0
management.server.port=0
