---
milestone: v1.0-rc-sdk (SDK scope of milestone v1.0 — M1 + M2 + M2.9 restructure)
audited: 2026-07-05
reconfirmed: 2026-07-06
status: passed
rc_cut: "v1.0-rc-sdk tag CUT at 4a4a202 (2026-07-05); CHANGELOG promoted [Unreleased]→[v1.0-rc-sdk] with a fresh empty [Unreleased]; drift gate OK (16 keys, 6 bands); working tree clean. The audit's one open Next action is now DONE — the SDK RC is closed. NOTE: this does NOT complete the full v1.0 milestone; the full v1.0 milestone is NOT complete — 38 platform reqs (INGEST/QUERY/HARD) remain correctly Pending."
scope_note: >
  This audit covers the SDK deliverable the v1.0-rc-sdk tag will cover — the Java SDK (M1)
  and Python SDK (M2), plus the M2.9 monorepo restructure. The full v1.0 milestone also
  contains the platform work (M3 ingest, M4 query/console, M5 hardening) which is intentionally
  NOT built yet — those requirements are correctly Pending and OUT OF SCOPE for an SDK RC.
scores:
  sdk_requirements: 25/25 (JSDK-01..11, CONT-01..03, PSDK-01..10, plus CI-01..05 + CI-PY-01..04 CI-floor)
  phases: 13/13 complete
  integration: green (C1–C12 on BOTH harnesses; drift gate; both example smokes)
  flows: green (emit → buffer → flush → OTLP/fallback proven per SDK; sample apps run E2E)
gaps:
  requirements: []      # none in SDK scope
  integration: []
  flows: []
tech_debt:
  - phase: 04.6-m2-6
    items:
      - "Pitfall #29 — OTLP force_flush swallows connection-refused; records against a dead/absent collector can be silently lost via the zero-arg handler default. Tracked SDK defect, documented in ADR-0020; not a blocker for the RC."
  - phase: benchmarks
    items:
      - "Realistic-redaction benchmark variant (populated redact_keys) is a documented carry-forward on both Java + Python overhead benchmarks."
  - phase: M2.9-restructure
    items:
      - "Registry publishing (Maven Central + PyPI) deferred — the RC is tag + GitHub release only; actual publish is later work."
      - "Nested Gradle coordinates (:sdk:java:core) deferred in favour of flat names to keep the M0 freeze untouched (ADR-0022)."
  - phase: stale-planning-metadata
    items:
      - "docs/REQUIREMENTS.md traceability still lists JSDK-*/CONT-*/PSDK-03/PSDK-10 as Pending, but M1 (v0.2-m1) and M2 (v0.3-m2) both shipped and are tagged — the table is stale, the work is done."
resolved_since_verification:
  - "02-m1-7 VERIFICATION was human_needed (JMH headline number a placeholder at scaffold time). RESOLVED: sdk-overhead.md carries the measured p99 6,360 ns; jmh-nightly ran green on main 2026-07-05 (artifact jmh-results-28721267796)."
  - "03.1-m1-9 VERIFICATION was human_needed (confirm pr-title-lint + gates ran on PR #26). RESOLVED: all Java CI gates have run green on every PR since; PR #37 (this session) passed the pr-title-lint gate."
missing_verification_docs:
  - "03-m1-8 (v0.2-m1 cut) and 04.7-m2-7 (v0.3-m2 cut) have no VERIFICATION.md — both are release-cut phases whose 'done' is the shipped git tag + GitHub release, both of which exist. Not treated as blockers."
---

# Milestone Audit — v1.0-rc-sdk (SDK scope)

## Verdict: ✅ PASSED (SDK scope) — clear to cut `v1.0-rc-sdk`

The two SDKs are feature-complete against the M0-frozen contract and conformance-tested. No
critical blockers. Accumulated tech debt (below) is tracked and none of it blocks an SDK release
candidate.

## Scope

`v1.0-rc-sdk` is a **release candidate for the SDK deliverable** — the Java SDK (M1) and Python
SDK (M2), now restructured under `sdk/` (M2.9). It is **not** the completion of the full
`v1.0` milestone: the platform milestones **M3 (ingest), M4 (query/console), M5 (hardening)** are
intentionally unbuilt, and their requirements (INGEST-*, QUERY-*, HARD-*) are correctly Pending
and out of scope here.

## Phase verifications (13/13)

| Phase | Status | Note |
|---|---|---|
| 01 M1.6 redactor/enricher | ✅ passed | C10/C11 (Java) green |
| 02 M1.7 logback/spring adapter | ⚠️→✅ human_needed **resolved** | bench number now measured + nightly green |
| 03 M1.8 v0.2-m1 cut | (no VERIFICATION) | shipped — tag `v0.2-m1` exists |
| 03.1 M1.9 Java CI hardening | ⚠️→✅ human_needed **resolved** | all gates green on every PR since |
| 04 M2.0 python scaffold | ✅ passed | C1/C12 (Python) green |
| 04.1–04.5 M2.1–M2.5 | ✅ passed (×5) | C2–C11 land incrementally; 20 passed at M2.5 |
| 04.6 M2.6 handler/sample/bench | ✅ passed | 127 unit + drift green; Pitfall #29 tracked |
| 04.7 M2.7 v0.3-m2 cut | (no VERIFICATION) | shipped — tag `v0.3-m2` exists |
| 04.8 M2.8 python CI floor | ✅ passed | ruff+mypy+cov gates green |

## Requirements coverage (SDK scope)

- **Java SDK (JSDK-01..11)** — shipped in `v0.2-m1`. Includes JSDK-10 overhead benchmark (p99
  6,360 ns, published). *(Traceability table stale-marks these Pending; the work is tagged done.)*
- **Cross-SDK contract (CONT-01..03)** — `config-keys.yaml` + `severity-table.json` + the drift
  CI gate; `check_contract_drift.py --sdk all` OK (16 keys, 6 bands) green on `main` this session.
- **Python SDK (PSDK-01..10)** — layered modules, contract-loaded config, non-blocking buffer,
  severity parity, `BeaconLoggingHandler`, SIGTERM drain, **C1–C12 green (20 passed)**, overhead
  benchmark (p99 30,663 ns), `v0.3-m2` cut. *(PSDK-03 ns-timestamp / PSDK-10 cut stale-marked
  Pending; both delivered.)*
- **CI floors** — Java CI-01..05 (M1.9) + Python CI-PY-01..04 (M2.8) all satisfied; M2.9 added the
  Python benchmark nightly to reach full Java parity (the gap the earlier scan flagged — now closed).

## Cross-phase integration & E2E (green on `main`, this session)

The conformance suite **is** the cross-SDK integration test — it proves the layered modules wire
together and that the Java and Python clients are interchangeable against one frozen contract:

- **Conformance:** C1–C12 green on BOTH harnesses (Java 12/12; Python **20 passed / 0 skipped**).
- **Drift gate:** `--sdk all` exit 0 (16 keys, 6 bands).
- **E2E smokes:** `examples/spring-boot-sample` (Java auto-config) and `examples/python-sample`
  (stdlib handler → resilience fallback, exit 0) both run end-to-end in CI.
- **M2.9 restructure:** verified green on `main` — `./gradlew build`, python conformance, drift,
  **both** benchmark nightlies (jmh + python-bench) succeeded with artifacts, and all push CI
  (`contract`, `java-sdk`, `python-sdk`) green.

## Tech debt / carry-forwards (non-blocking)

1. **Pitfall #29** — OTLP `force_flush` swallows connection-refused (tracked SDK defect, ADR-0020).
2. Realistic-redaction benchmark variant (both SDKs) — documented carry-forward.
3. Registry publishing (Maven Central + PyPI) — deferred; the RC is tag + GitHub release only.
4. Stale `docs/REQUIREMENTS.md` traceability (Pending marks for shipped JSDK/CONT/PSDK rows).

## Next

~~Cut `v1.0-rc-sdk`~~ — **DONE** (2026-07-06 reconfirm). Tag `v1.0-rc-sdk` cut at `4a4a202`;
CHANGELOG `[Unreleased]` → `[v1.0-rc-sdk] — 2026-07-05` with a fresh empty `[Unreleased]`; drift
gate green; tree clean. The SDK release candidate is closed.

**The full milestone v1.0 is NOT complete** — this was an SDK-scoped RC, not the whole
milestone. the full v1.0 milestone must **not** be considered complete yet: the platform arc (M3 ingest,
M4 query/console, M5 hardening — 38 Pending INGEST/QUERY/HARD requirements) is unbuilt and
intentionally out of scope for the RC.

Two real forward paths remain, both defined in the roadmap:
- **Phase 4.9 / M2.9 publishing** — Maven Central + PyPI + doc sites (PUB-01..04). The tagged
  "M2.9" so far is only the monorepo *restructure*; registry publishing is still deferred
  (tech-debt item #3 above). This makes the SDKs externally installable.
- **Phase 5 / M3.0 ingest skeleton** — begins the platform arc toward the full `v1.0`.
