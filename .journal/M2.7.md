# M2.7 journal — v0.3-m2 release cut + M2 retrospective + tag

**Branch:** `gsd/phase-04.7-m2-7-v0-3-m2-release-cut-retrospective-tag` off `main@f07833b`
**Goal:** Non-conformance deliverable — cut the `v0.3-m2` release: green-tree proof, `docs/M2-COMPLETE.md` retrospective, `.journal/M2.7.md`, CHANGELOG `[v0.3-m2]` promotion, roadmap-status refresh, git tag + GitHub Release.
**Plan:** `.planning/phases/04.7-m2-7-.../04.7-01-PLAN.md` (this plan) + `04.7-02-PLAN.md` (CHANGELOG promotion + tag).

## What I did

- Captured the green-tree proof BEFORE any release paperwork: Python conformance `20 passed / 0 skipped` (C1–C12 all green), frozen `grep -c 'def test_c[0-9]'` still `13` (M0 scenario list untouched), `check_contract_drift.py` exit 0 for `--sdk java`, `--sdk python`, and `--sdk all` (16 key entries, 6 bands each), and `./gradlew :conformance-java:test` BUILD SUCCESSFUL so the tag covers a coherent multi-SDK tree.
- Wrote `docs/M2-COMPLETE.md` — the four-section M2 retrospective mirroring `docs/M1-COMPLETE.md`'s shape (what was harder / what conformance caught / v2 resilience carry-list / what M3+ inherits), prose not a note-dump, sourced from the M2.0–M2.8 CHANGELOG blocks + the M2.x journals.
- Wrote this journal per CLAUDE.md § Per-phase "done" definition (six canonical sections).
- Handed off to Plan 04.7-02: promote CHANGELOG `[Unreleased]` (M2.0–M2.8) → `[v0.3-m2]` + reset an empty `[Unreleased]`; refresh the status lines in `docs/M2-ROADMAP.md` / `docs/ROADMAP.md` / `.planning/ROADMAP.md`; cut `git tag v0.3-m2` + the GitHub Release (human checkpoint).
- Phase-order oddity noted throughout: M2.8 (Phase 4.8) landed BEFORE this cut, so `[Unreleased]` already spans M2.0–M2.8 and the tag points at a full-gate-passing tree — the reorder was deliberate (ADR-0021), not an accident.

## Problems I faced

The 4.8-before-4.7 reorder means the CHANGELOG `[Unreleased]` section spans M2.8 at the TOP down to M2.0 at the bottom — Plan 04.7-02 has to verify the FULL M2.0–M2.8 span is present before promoting, so no M2.x block gets stranded above the freshly-reset `[Unreleased]`.

The 12/12-vs-20-passed mapping needed confirming: the harness reports 20 passed / 0 skipped because it carries the nine C1 negative-schema fixtures + the C12 severity parametrization on top of the 13 `test_c*` functions — 12/12 *scenarios* green (C1–C12 all un-skipped since M2.5) is the real bar, not the raw test count.

Criterion 5 (drift gate both SDKs) turned out to be already-satisfied rather than a build item: `check_python_sdk` already exists (the `--sdk python` path is no longer the M1.8 no-op stub), and `python-sdk.yml` already runs it — the temptation was to "add" drift coverage that was already there, which the plan explicitly warns against.

And the tag itself must NOT be cut before the PR merges + CI is green on `main`: a release cut cannot tag ahead of merge (no direct commits to `main`; feature branch + PR; tag lands on the merge commit) — that lives in 04.7-02's human checkpoint.

## What could have been done better

The retrospective's v2 carry-list overlaps M1's almost verbatim (synchronous retry on the flusher thread, the regex-over-source drift checker) — a single shared cross-SDK carry-list doc would be cleaner than two parallel `*-COMPLETE.md` files each repeating the same v2 asks. And the OTLP `force_flush` fallback-swallow (Pitfall #29) is a real defect shipping under a green tag — arguably it should have been closed in M2.6 rather than deferred, but the collector-free sample demonstrates the real `ResilientSink → file` fallback path, so the tag is at least honest about what works and what doesn't.

## Changes carried back to earlier phases

None functional — a release cut is docs + status + tag only. The only edits are the CHANGELOG consolidation (04.7-02), the roadmap-status refreshes, and the ADR-index / roadmap cross-links that were already added incrementally in M2.1–M2.8. No SDK source, no CI code, no conformance harness touched (criterion 5 was already satisfied in the codebase, so nothing was built here).

## What's next

- **Hand-off to next phase** — Plan 04.7-02 promotes the CHANGELOG, refreshes the three roadmap status lines, and cuts `git tag v0.3-m2` + the GitHub Release at a human checkpoint. The Release links to `docs/M2-COMPLETE.md` as the senior-engineer artifact; the tag must land on the `main` merge commit after CI is green.
- **v2 carry-list** — M2.9 (Phase 4.9) cross-SDK publishing (Maven Central + PyPI + docs sites), then M3.0 (Phase 5) the platform ingest skeleton (Gateway + Kafka + Indexer + ES). Still tracked for a dedicated future phase: the OTLP `force_flush` fallback-swallow defect (Pitfall #29) and the synchronous-retry-on-flusher-thread carry (Pitfall #25), both now doubled across the two SDKs.

## Journal

The whole point of splitting this plan out was discipline: write the retrospective and lock the green-tree proof BEFORE the irreversible tag, so the docs get reviewed against a verified-green tree rather than being backfilled after the fact. The phase-order inversion still feels slightly odd in the numbering — shipping the CI hardening floor (4.8) before the release cut (4.7) — but it's the right call: the tag is gate-clean because the gates were turned on first. The "already done" surprise on criterion 5 was a good reminder to VERIFY before BUILD — `check_python_sdk` was sitting there fully implemented from an earlier phase, and adding a second copy would have been pure waste. Running the Java conformance locally (JVM 25 present) was worth the eleven seconds: it means the tag genuinely covers both SDKs, not just the Python side I've been living in for nine phases. The honesty tax on Pitfall #29 is real — tagging a milestone with a documented fallback-swallow defect open feels uncomfortable, but hiding it would be worse, and the sample proves the fallback path works when wired explicitly. Green-tree proof captured, both docs written, handing the CHANGELOG promotion + tag to 04.7-02.
